#include <stdio.h>

void main(){
    int i = 0;
    int soma = 0;
    
    while(i <= 100){
         soma = soma + i;
        // printf("%d\n", i);
        printf("\nPos: %d, Soma: %d", i, soma);
        i++;
    }
    
    
    
}